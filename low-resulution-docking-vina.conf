# AutoDock Vina Configuration File
# Define the search space (box) center and size (in Angstroms)

# Center of the docking box (coordinates x, y, z)
center_x = 10.22
center_y = 16.18
center_z = 1.04

# Size of the docking box (dimensions x, y, z)
size_x = 15.91
size_y = 15.00
size_z = 15.00

# Optional: Exhaustiveness (default = 8)
exhaustiveness = 16

# Optional: Number of binding modes to output (default = 9)
num_modes = 10

# Optional: Energy range (kcal/mol, default = 3)
energy_range = 5
